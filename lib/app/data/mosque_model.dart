class MosqueModel {
  String nama;
  String alamat;
  String sourceID;
  String layerID;
  double latitude;
  double longitude;
  double distance;
  MosqueModel(this.nama, this.alamat, this.sourceID, this.layerID,
      this.latitude, this.longitude, this.distance);
}
